<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:aop="http://www.springframework.org/schema/aop"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
         http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd">
        

    <context:component-scan base-package="com.yc.bean.impl,com.yc.bean,com.yc.biz.impl"/>
    <!-- 切面注解解析器 -->
    <aop:aspectj-autoproxy/>
    
    <!-- 数据库连接文件db.properties的读取
    spring的容器启动时，会以流的方式读取这个配置信息 -->
    <context:property-placeholder location="db.properties"/>
    <!-- spring整合mybatis/bibernate/jdbc/jpa的配置思路:
    	1.配置数据源(->数据连接池) ——>2.如果使用dao框架，都要求配置
     -->
    <!-- 使用dbcp配置数据库连接池 -->
    <bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource" destroy-method="close">
    <property name="driverClassName" value="${jdbc.driver}"/>
    <property name="url" value="${jdbc.url}"/>
    <property name="username" value="${jdbc.username}"/>
    <property name="password" value="${jdbc.password}"/>
	</bean>
	
	<!-- 取得sqlSessionFactory     将DataSource与myBatis配置文件一起用于生成一个SqlSessionFactory-->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource"></property>
		<!-- 配置mybatis中的configuration中的setting设置 -->
		<!-- 日志 -->
		<property name="configurationProperties">
			<props>
				<prop key="logImpl">LOG4J</prop>
			</props>
		</property>
		<!-- 别名的包的设置 -->
		<property name="typeAliasesPackage" value="com.yc.bean"></property>
		<!-- 配置mapper映射文件 -->
		<property name="mapperLocations" value="classpath*:/com/yc/mapper/*.xml"></property>
	</bean>

</beans>